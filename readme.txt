in pyline-bot
	just sources, not main code
		app.py = template totaly works
		template-complete-bot.py = lot of option for linebot (not for used), take code separately
		api = api h3h3

	folder python tools
		its only things to learn in python

	folder pyline-bot-master
		main code for line bot (including mqtt broker, and line notify)

	folder backup
		also a main code, not using any external code like mqtt broker and line notify

in pyline-nono
	how to send text from python to line notify

	set line notify api
		this tutorial based on
			https://www.youtube.com/watch?v=e-pHyMILcZE
		
		https://notify-bot.line.me/my/
			klik generate token